import React, { FC } from 'react';
import Button from '@mui/material/Button';

const App: FC = () => {
  return (
    <div>
      this is app.
      <Button>Hello</Button>
    </div>
  );
};

export default App;
